# 청첩장 리뷰 (2026-08-09)

| 항목 | 내용 |
| --- | --- |
| 검토 대상 | `http://127.0.0.1:5173` (로컬 dev), `~/Desktop/Project/wedding-invitation` 소스 |
| 비교 기준 | `https://tak0407.github.io/wedding-invitation/` (배포본, vanilla HTML) |
| 검토 시점 | 2026-08-09 20:23 KST |
| 마지막 커밋 | `c742419` 햇살 오프닝 연출 추가 (React 마이그레이션은 미커밋 상태) |
| 검토 환경 | Playwright, 390x844 (모바일) 및 1280x900 (데스크톱) |

> 검토 중 `src/App.jsx`가 계속 수정되고 있었다 (mtime이 검토 시점 21초 전). 아래는 위 시점의 스냅샷 기준이며, 이후 수정분은 반영되지 않았다.

---

## 요약

디자인 완성도는 충분히 높다. 남은 작업은 대부분 "안 터지게, 제대로 전달되게" 쪽이다.

**다음 3개를 먼저 처리하기를 권한다.**

1. 스냅샷 커밋 + 의존성 버전 고정 (작업물 보호)
2. `chandelier.png` 1.66MB를 webp로 되돌리고 리사이즈
3. OG 태그 추가 + 배포 워크플로우 정리

---

## P0. 배포 전 반드시

### [ ] P0-1. OG 태그가 하나도 없다

`index.html` `<head>`에 `og:title` / `og:description` / `og:image`가 전무하다. 청첩장은 사실상 전량 카카오톡으로 전달되는데, 지금 링크를 보내면 썸네일도 설명도 없는 맨 URL 카드로 뜬다. 공들인 커버 디자인이 정작 공유 지점에서 보이지 않는다.

- `og:image` 1200x630 (커버 아치창 크롭 권장)
- `og:title` `온유 ♥ 봄` 대신 실명 기준으로 `김경탁 ♥ 금정민 · 2027.08.28`
- `og:description` 날짜, 시각, 장소
- `twitter:card=summary_large_image`
- `og:url`, `canonical`

### [ ] P0-2. 샹들리에 이미지 1.66MB (회귀)

| | 이전 (배포본) | 현재 (로컬) |
| --- | --- | --- |
| 파일 | `chandelier.webp` | `chandelier.png` |
| 크기 | 262 KB | **1,661 KB** |
| 빌드 산출물 | 해당 없음 | `dist/assets/chandelier.png` 1,212 KB |

커버 최상단, 즉 첫 화면을 막는 이미지다. 이미지 퀄리티 개선은 유지하되 포맷만 webp/avif로 되돌리고 실제 표시 크기(모바일 기준 300px 내외)에 맞춰 리사이즈하면 100KB 안쪽으로 떨어진다.

참고로 현재 에셋 총량은 다음과 같다.

```
chandelier.png     1,661,527 bytes
arch-window.webp     200,532 bytes
rose-garland.webp     75,266 bytes
divider.webp          12,868 bytes
```

### [ ] P0-3. 배포 경로가 끊겨 있다

- GitHub Pages는 `main` 브랜치 루트를 서빙한다.
- 새 앱은 `dist/`로 빌드되고, `dist/`는 `.gitignore`에 들어 있다.
- `.github/workflows`가 없다.

즉 현재 구조로는 아무리 완성해도 `tak0407.github.io/wedding-invitation/`에 반영되지 않는다. Actions 워크플로우를 추가하거나 `gh-pages` 브랜치를 쓰거나, 배포 방식을 먼저 정해야 한다.

### [ ] P0-4. React 마이그레이션 전체가 미커밋이다

```
?? src/  ?? public/  ?? package.json  ?? vite.config.js  ?? .gitignore
 D assets/arch-window.webp
 D assets/chandelier.webp
 D assets/corner.webp
 D assets/divider.webp
 D assets/rose-garland.webp
 M index.html
```

마지막 커밋은 아직 vanilla 버전이고, 새 작업은 전부 untracked, 기존 에셋은 삭제 표시 상태다. 지금 파일 하나를 잘못 날리면 복구 지점이 없다. 다음 작업 지시 전에 스냅샷 커밋을 먼저 찍는 편이 안전하다.

---

## P1. 품질, 안정성

### [ ] P1-1. 에러 바운더리가 없다

검토 중 실제로 다음이 발생했고, 페이지 전체가 백지가 됐다.

```
ReferenceError: reduceMotion is not defined
  at Ceremony (App.jsx:656)
An error occurred in the <Ceremony> component.
Consider adding an error boundary to your tree...
```

편집 중간 상태여서 현재는 복구됐다. 다만 구조적으로, 컴포넌트 하나가 예외를 던지면 청첩장 전체가 백지가 된다. vanilla였을 때는 불가능했던 실패 모드다. 하객은 백지를 보면 새로고침 없이 그냥 닫는다. 최상위 ErrorBoundary는 넣어두는 편이 좋다.

### [ ] P1-2. 의존성이 전부 `"latest"`

```json
"react": "latest", "react-dom": "latest",
"framer-motion": "latest", "vite": "latest",
"@vitejs/plugin-react": "latest"
```

재현 불가능한 빌드다. 2027년 8월까지 살아 있어야 하는 페이지인데, 어느 시점에 `npm install` 하면 빌드가 깨질 수 있다. 버전 고정 권장.

### [ ] P1-3. 한글 어절 중간 끊김 (`word-break: keep-all` 없음)

배포본에서 "진장디플렉스 주차장)"이 `주차` / `장)` 으로 두 줄에 걸쳐 끊기는 것을 확인했다. 현재 로컬 문안에서는 우연히 드러나지 않지만, 규칙 자체가 없으므로 문구가 바뀌면 언제든 재발한다.

```css
body { word-break: keep-all; overflow-wrap: anywhere; }
```

### [ ] P1-4. 번들 크기

| 항목 | 크기 |
| --- | --- |
| `dist/assets/index-*.js` | 323,906 bytes (gzip 102,122) |

정적 청첩장 한 장에 React + framer-motion이 붙어 있다. 이미지까지 합치면 첫 로드가 1.5MB를 넘는다. 참고로 이전 vanilla 버전은 동일한 연출(리빌, 카운트업, 보케, reduced-motion 대응)을 CSS와 IntersectionObserver만으로 이미 구현하고 있었다. 유지보수 편의가 목적이면 유지해도 되지만, 얻은 것이 무엇인지는 한 번 따져볼 만하다.

### [ ] P1-5. `.ics` 를 `<a download>` 로 제공

```jsx
<a className="action-link" href={asset("wedding.ics")} download>일정 저장</a>
```

카카오톡 인앱 브라우저에서 `download` 속성이 정상 동작하지 않는 경우가 많다. 청첩장 트래픽 대부분이 카톡 인앱인데 "일정 저장"이 무반응일 수 있다. 실기기 카톡 테스트가 필요하다.

### [ ] P1-6. SCROLL 힌트가 첫 화면 밖으로 잘린다

측정값 (390x844 뷰포트 기준)

| 항목 | 값 |
| --- | --- |
| `.scroll-hint` 위치 | 836 ~ 898 px |
| 뷰포트 높이 | 844 px |
| 초안 배지 높이 | 31.8 px |

배지를 제거해도 804 ~ 866px이라 여전히 잘린다. 배지 탓이 아니라 커버(`min-height: 100svh`) 레이아웃 자체 문제다. 스크롤을 유도하는 요소가 스크롤해야 보이는 상태다.

---

## P2. 접근성, 가독성

### [ ] P2-1. 텍스트 대비가 WCAG AA(4.5:1) 미달

아이보리 배경(`#FDFBF6`) 기준 실측값이다.

| 토큰 | 값 | 대비 | 판정 | 주요 사용처 |
| --- | --- | --- | --- | --- |
| `--ink` | `#37312A` | 12.42 | 통과 | 본문 |
| `--brass-deep` | `#8F7040` | 4.45 | 경계 | eyebrow |
| `--ink-soft` | `#877D6C` | **3.92** | **미달** | 교통편 본문(14.5px), 예식 시각, D-day, 갤러리 캡션, 푸터 |
| `--blush-deep` | `#C28A93` | **2.76** | **미달** | 달력 일요일 |
| `--brass-soft` | `#C9AE7E` | 2.06 | 미달 | 장식 위주 |

`--ink-soft`가 하필 오시는 길 교통편처럼 가장 실용적으로 중요한 텍스트에 쓰인다. 하객 연령대를 고려하면 실질적인 문제다. `#6B6355` 정도로만 어둡게 해도 4.5를 넘기고 톤은 거의 상하지 않는다.

### [ ] P2-2. 문서 구조

- `<h1>`이 없다.
- `<h2>`는 "오시는 길" 하나뿐이다.
- 나머지 섹션 제목이 전부 영문 eyebrow(`<p class="eyebrow">INVITATION`)로만 존재해서 스크린리더로 섹션 탐색이 되지 않는다.

커버의 신랑신부 이름을 `h1`으로 올리고, 각 섹션에 시각적으로 숨긴 `h2`를 넣는 방식을 권한다.

### [ ] P2-3. `aria-label`을 plain `<div>`에 걸었다

```jsx
<div className="parents" aria-label="혼주 성함">
<motion.div className="action-row" aria-label="예식 일정 저장">
```

`role` 없는 `div`의 `aria-label`은 보조기기에서 무시된다. 접근성 표시만 있고 실효는 없다.

### [ ] P2-4. 자잘한 것

- favicon 404 (`/favicon.ico`)
- TEL 052-289-9851이 텍스트다. `tel:` 링크로 만들면 모바일에서 바로 걸린다
- 갤러리 라이트박스(탭 확대) 없음

---

## 확인 필요 (사실관계)

### [ ] C-1. 혼주 표기 불일치

```
김임수 · 정예원의 장남 경탁
금병서 · 배태숙의 딸  정민
```

한쪽만 서열(장남)을 표기했다. 장남/장녀로 통일하거나 둘 다 빼는 쪽으로 맞춰야 한다.

### [ ] C-2. "하객 주차는 최대 2시간 30분 무료"

교통편에서 유일하게 남은 미검증 구체 수치다. 틀리면 예식 당일 하객 컴플레인으로 직결된다. 홀에 전화로 확인 권장.

### [ ] C-3. 루미에르홀

홀 공식 사이트(`ulsan.jwwedding.kr`)와 웹 검색에서 확인되는 울산 JW컨벤션의 홀은 **그랜드볼룸, 마제스틱홀** 두 개뿐이고 "루미에르"는 나오지 않는다.

문제는 `index.html` 최상단 CSS 주석이다.

```
/* 루미에르홀 리서치 기반: 아이보리·대리석, 아치 통창,
   크리스탈 샹들리에, 황동, 블러쉬 플라워, 월넛 도어 */
```

디자인 콘셉트 전체가 이 "리서치" 위에 서 있는데 출처가 불분명하다. 홀 이름이 맞더라도 저 인테리어 묘사가 실제와 일치하는지는 별개 문제다. 실물 사진으로 한 번 대조하기를 권한다.

### [ ] C-4. 계좌, 연락처 섹션 제외 여부

배포본에 있던 "마음 전하실 곳"과 "연락처" 섹션이 로컬에서는 통째로 빠져 있다 (배지 문구: "웨딩 사진·연락처는 확인 후 추가"). 의도된 것이면 문제없고, 최종 스코프만 정해두면 된다. 추가한다면 다음도 같이 결정이 필요하다.

- 혼주(부모) 계좌 포함 여부
- 예금주 표기
- 복사 버튼이 계좌번호만 복사할지, 은행명 포함 문자열을 복사할지

---

## 반영 확인된 것

- **지도 딥링크** 카카오맵, 네이버지도 버튼 추가
- **일정 저장(.ics) + Google Calendar** 추가. 시각 검증 결과 정확하다
    - `DTSTART:20270828T033000Z` = KST 2027-08-28 12:30 (정확)
    - `DTEND:20270828T053000Z` = 2시간 (정확)
    - `.ics`의 SUMMARY, LOCATION, DESCRIPTION 모두 정상
- **미검증 교통편 제거**. 701·916번, 5003 리무진, 병영사거리 등 구체 정보를 "태화강역에서 버스 또는 택시", "지도 앱에서 최신 경로 확인"으로 교체했다. 틀린 정보를 싣느니 이쪽이 맞다
- **D-day 카운터 스크롤 트리거 복구** (`useInView`). 검토 도중 수정됐다
- **CSS 이관이 깔끔하다.** `html.js .reveal { opacity: 0 }` 규칙을 제대로 제거해서 framer-motion과의 이중 제어 충돌이 없다. 이 부분을 안 지웠으면 섹션이 영영 안 보이는 사고가 났을 것이다

---

## 비주얼 개선 제안

기능 결함이 아니라 디자인 관점의 의견이다. 우선순위는 V-1, V-5, V-4 순으로 본다.

### V-1. 빈 갤러리가 페이지에서 가장 큰 섹션이다

섹션별 높이 실측값이다 (390px 폭 기준).

| 섹션 | 높이 | 비중 |
| --- | --- | --- |
| cover | 895 px | 17% |
| interlude | 406 px | 8% |
| greeting | 674 px | 13% |
| ceremony | 904 px | 18% |
| **gallery** | **1,044 px** | **20%** |
| directions | 870 px | 17% |
| footer | 307 px | 6% |

사진이 없는 현재 상태에서 빈 아치 한 개와 4칸 그리드가 페이지 최대 면적을 차지한다. 결과적으로 "미완성"이 가장 크게 보인다. 촬영 전까지는 섹션을 통째로 숨기거나, 히어로 1개 + 2칸 정도로 줄이는 편이 낫다.

### V-2. 섹션 리듬이 없다

모든 섹션이 `padding: 84px 30px` + 디바이더 + eyebrow 라는 동일 패턴으로 시작한다. 표지 이후로 끝까지 밀도가 균질해서 강약이 없다.

청첩장은 "표지(강) → 인사말(약) → 날짜(강) → 오시는 길(정보)" 처럼 밀도가 출렁여야 읽는 리듬이 생긴다. 지금은 카탈로그에 가깝다. 인사말 같은 정서 구간은 여백을 더 주고, 오시는 길 같은 정보 구간은 조여도 된다.

### V-3. 디바이더 일러스트가 5회 반복된다

동일한 `divider.webp`가 모든 섹션 상단에 같은 크기로 들어간다. 처음에는 우아하지만 다섯 번째에는 템플릿처럼 읽힌다.

- 두 곳 정도만 남기고 나머지는 얇은 선이나 여백으로 대체
- 또는 섹션마다 폭과 크기를 다르게

### V-4. eyebrow가 전부 영문이라 정보량이 0이다

`INVITATION` / `CEREMONY` / `MOMENTS` / `DIRECTIONS` 네 개가 동일한 `letter-spacing: 0.46em` 대문자 스타일로 반복된다. 한글 청첩장인데 섹션 라벨이 장식 역할만 하고 정보를 주지 않는다.

"예식 안내", "오시는 길" 같은 한글을 주 라벨로 올리고 영문을 작게 부제로 내리면 위계도 생기고 정보도 산다.

### V-5. D-day 숫자가 부속 텍스트처럼 보인다

"예식까지 **384**일 남았습니다"에서 문제가 두 가지다.

- Cormorant의 old-style numerals라 3과 4가 베이스라인 아래로 내려가서 한글과 어긋나 보인다
- 색이 `--ink-soft` 회색이고 크기도 15px 본문 수준이다

카운트업 애니메이션까지 구현해 놓고 정작 시각적으로는 눈에 띄지 않는다. D-day는 청첩장에서 감정적 훅에 해당하니 숫자만 28~32px, 진한 색, lining figures로 키우는 편이 좋다.

### V-6. 액션 버튼에 위계가 없고 라벨 언어가 섞였다

"일정 저장"(한글)과 "Google Calendar"(영문)가 완전히 동일한 아웃라인 필 스타일이다. 무엇이 주 액션인지 알 수 없다. 또한 이 두 버튼이 페이지에서 유일하게 "웹 UI처럼 생긴" 요소라 인쇄물 톤에서 튄다.

- 주 액션 하나만 채워진(filled) 스타일로
- 라벨 언어 통일 ("캘린더에 저장" / "구글 캘린더")

### V-7. 본문 서체 때문에 커버만 청첩장처럼 보인다

이름과 날짜는 Gowun Batang 명조인데 본문은 Pretendard 고딕이다. 그래서 표지는 청첩장인데 스크롤 이후는 웹페이지처럼 읽힌다.

인사말 문단까지 명조로 올리면 톤이 훨씬 붙는다. 가독성이 걱정되면 인사말은 명조, 오시는 길 같은 실용 정보는 고딕으로 나누는 절충도 가능하다.

### V-8. 색이 사실상 단색이다

`--blush: #E5C2C6`를 정의해 두고 실제로는 장미 일러스트에서만 쓰인다. UI에 쓰이는 색은 아이보리와 브라스뿐이다. 고급스럽지만 조금 차갑다.

28일 마커, 인용부, 인터루드 배경 등에 블러쉬를 아주 옅게 깔면 온기가 생긴다.

### V-9. 커버 세로가 빡빡하다

샹들리에, 아치창, 날짜, 시각과 장소, SCROLL을 `100svh` 안에 모두 넣으려다 보니 SCROLL이 밖으로 밀려났다 (P1-6과 같은 원인). 샹들리에를 10~15% 줄이거나 아치창 하단 창틀을 조금 크롭하면 여유가 생긴다.

### V-10. 아치창 안 배경이 침엽수 숲이다

콘셉트("통창으로 드는 정오의 빛")와는 맞지만, 울산 도심 컨벤션홀이라는 실제 장소감과는 어긋난다. 유럽 숲 풍경이라 미묘한 위화감이 있다. 추상적인 빛 그라데이션이나 흐릿한 정원 실루엣이 더 안전하다.

### V-11. 데스크톱 좌우 여백이 죽어 있다

430px 카드 양옆이 marble 단색으로 1,000px 가까이 비어 있다. 커버 이미지를 크게 블러 처리해 깔거나, 대리석 텍스처, 최소한 미세한 그라데이션이라도 넣으면 "모바일 페이지를 데스크톱에서 억지로 본다"는 느낌이 줄어든다.

다만 하객 대부분이 모바일로 열 것이므로 우선순위는 낮다.

---

## 검증 로그

정확성 확인 결과다.

| 항목 | 결과 |
| --- | --- |
| 2027-08-28이 토요일인지 | 정확 |
| 8월 달력 배치 (1일 일요일 시작, 31일까지) | 정확 (`cal 8 2027` 대조) |
| D-day 384일 | 정확 (2026-08-09 기준) |
| 주소 "울산광역시 북구 진장유통로 35" | 실제와 일치 |
| 전화 052-289-9851 | 실제와 일치 |
| Google Calendar URL 시각 파라미터 | 정확 |
| `.ics` DTSTART / DTEND | 정확 |
| 루미에르홀 존재 여부 | **확인 필요** (C-3) |
| 주차 2시간 30분 무료 | **확인 필요** (C-2) |

기타 참고 사항이다.

- `vite.config.js`의 `base: "./"` 는 GitHub Pages 프로젝트 페이지에서 정상 동작한다. 문제는 base가 아니라 배포 경로다 (P0-3).
- 장식 이미지 전부 `alt=""` 처리가 되어 있다. 올바르다.
- `prefers-reduced-motion` 대응이 보케 캔버스, 리빌, 카운트업까지 전 구간에 들어가 있다. 잘 되어 있다.
- 달력이 `<table>` + `<caption>` + `scope` 로 시맨틱하게 짜여 있다. 잘 되어 있다.
